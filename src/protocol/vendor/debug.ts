// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               v7.35.1
// source: debug.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "debug";

/**
 * Frontend -> WARDuino.
 * The receiver selects the payload schema from the command byte.
 */
export enum Command {
  /** COMMAND_RUN - no payload */
  COMMAND_RUN = 0,
  /** COMMAND_HALT - no payload */
  COMMAND_HALT = 1,
  /** COMMAND_PAUSE - no payload */
  COMMAND_PAUSE = 2,
  /** COMMAND_STEP - no payload */
  COMMAND_STEP = 3,
  /** COMMAND_STEP_OVER - no payload */
  COMMAND_STEP_OVER = 4,
  /** COMMAND_ADD_BREAKPOINT - Breakpoint */
  COMMAND_ADD_BREAKPOINT = 5,
  /** COMMAND_REMOVE_BREAKPOINT - Breakpoint */
  COMMAND_REMOVE_BREAKPOINT = 6,
  /** COMMAND_DUMP - no payload */
  COMMAND_DUMP = 7,
  /** COMMAND_DUMP_LOCALS - no payload */
  COMMAND_DUMP_LOCALS = 8,
  /** COMMAND_SNAPSHOT - no payload */
  COMMAND_SNAPSHOT = 9,
  /** COMMAND_DUMP_EVENTS - Range */
  COMMAND_DUMP_EVENTS = 10,
  /** COMMAND_DUMP_CALLBACKS - no payload */
  COMMAND_DUMP_CALLBACKS = 11,
  /** COMMAND_UPDATE_FUNCTION - Function */
  COMMAND_UPDATE_FUNCTION = 12,
  /** COMMAND_UPDATE_LOCAL - ValueUpdate */
  COMMAND_UPDATE_LOCAL = 13,
  /** COMMAND_UPDATE_CALLBACKS - CallbackMapping */
  COMMAND_UPDATE_CALLBACKS = 14,
  /** COMMAND_UPDATE_MODULE - ModuleUpdate */
  COMMAND_UPDATE_MODULE = 26,
  /** COMMAND_UPDATE_GLOBAL - ValueUpdate */
  COMMAND_UPDATE_GLOBAL = 27,
  /** COMMAND_UPDATE_STACK - ValueUpdate */
  COMMAND_UPDATE_STACK = 28,
  /** COMMAND_LOAD_SNAPSHOT - Snapshot */
  COMMAND_LOAD_SNAPSHOT = 15,
  /** COMMAND_PROXIFY - no payload */
  COMMAND_PROXIFY = 16,
  /** COMMAND_ADD_PROXY - FunctionRef */
  COMMAND_ADD_PROXY = 17,
  /** COMMAND_REMOVE_PROXY - FunctionRef */
  COMMAND_REMOVE_PROXY = 18,
  /** COMMAND_PROXY_CALL - RemoteFunctionCall */
  COMMAND_PROXY_CALL = 19,
  /** COMMAND_POP_EVENT - no payload */
  COMMAND_POP_EVENT = 20,
  /** COMMAND_PUSH_EVENT - Event */
  COMMAND_PUSH_EVENT = 21,
  /** COMMAND_CONTINUE_FOR - ContinueFor */
  COMMAND_CONTINUE_FOR = 22,
  /** COMMAND_INSPECT - Inspect */
  COMMAND_INSPECT = 23,
  /** COMMAND_RESET - no payload */
  COMMAND_RESET = 24,
  /** COMMAND_INVOKE - RemoteFunctionCall */
  COMMAND_INVOKE = 25,
  /** COMMAND_SET_SNAPSHOT_POLICY - SnapshotPolicyConfig */
  COMMAND_SET_SNAPSHOT_POLICY = 29,
  /** COMMAND_SET_OVERRIDE - Override */
  COMMAND_SET_OVERRIDE = 30,
  /** COMMAND_REMOVE_OVERRIDE - Override */
  COMMAND_REMOVE_OVERRIDE = 31,
  UNRECOGNIZED = -1,
}

/**
 * WARDuino -> frontend.
 * The receiver selects the payload schema from the notification byte.
 */
export enum NotificationType {
  /** NOTIFICATION_CONTINUED - no payload */
  NOTIFICATION_CONTINUED = 0,
  /** NOTIFICATION_HALTED - no payload */
  NOTIFICATION_HALTED = 1,
  /** NOTIFICATION_PAUSED - no payload */
  NOTIFICATION_PAUSED = 2,
  /** NOTIFICATION_STEPPED - no payload */
  NOTIFICATION_STEPPED = 3,
  /** NOTIFICATION_HIT_BREAKPOINT - HitBreakpoint */
  NOTIFICATION_HIT_BREAKPOINT = 4,
  /** NOTIFICATION_NEW_EVENT - NewEvent (zero-length payload) */
  NOTIFICATION_NEW_EVENT = 5,
  /** NOTIFICATION_FUNCTION_DUMP - Function */
  NOTIFICATION_FUNCTION_DUMP = 6,
  /** NOTIFICATION_LOCALS_DUMP - Locals */
  NOTIFICATION_LOCALS_DUMP = 7,
  /** NOTIFICATION_SNAPSHOT - Snapshot */
  NOTIFICATION_SNAPSHOT = 8,
  /** NOTIFICATION_EVENTS_DUMP - EventsQueue */
  NOTIFICATION_EVENTS_DUMP = 9,
  /** NOTIFICATION_CALLBACKS_DUMP - CallbackMapping */
  NOTIFICATION_CALLBACKS_DUMP = 10,
  /** NOTIFICATION_CHANGE_AFFECTED - no payload */
  NOTIFICATION_CHANGE_AFFECTED = 11,
  /** NOTIFICATION_MALFORMED - no payload */
  NOTIFICATION_MALFORMED = 12,
  /** NOTIFICATION_UNKNOWN_COMMAND - no payload */
  NOTIFICATION_UNKNOWN_COMMAND = 13,
  /** NOTIFICATION_OPERATION_RESULT - OperationResult */
  NOTIFICATION_OPERATION_RESULT = 14,
  /** NOTIFICATION_REMOTE_FUNCTION_RESULT - RemoteFunctionResult */
  NOTIFICATION_REMOTE_FUNCTION_RESULT = 15,
  /** NOTIFICATION_CHECKPOINT - Checkpoint */
  NOTIFICATION_CHECKPOINT = 16,
  UNRECOGNIZED = -1,
}

export enum State {
  STATE_WARDUINO_RUN = 0,
  STATE_WARDUINO_PAUSE = 1,
  STATE_WARDUINO_STEP = 2,
  STATE_PROXY_RUN = 3,
  STATE_PROXY_HALT = 4,
  UNRECOGNIZED = -1,
}

export enum SnapshotPolicy {
  SNAPSHOT_POLICY_NONE = 0,
  SNAPSHOT_POLICY_EVERY_INSTRUCTION = 1,
  SNAPSHOT_POLICY_CHECKPOINTING = 2,
  UNRECOGNIZED = -1,
}

/** A virtual program address. */
export interface CodeLocation {
  moduleIndex: number;
  programCounter: number;
}

export interface Breakpoint {
  location?: CodeLocation | undefined;
}

export interface HitBreakpoint {
  location?: CodeLocation | undefined;
}

/**
 * The notification type carries all information for this event. The empty
 * message exists for host-side reflection, but no protobuf bytes are sent.
 */
export interface NewEvent {
}

export interface ContinueFor {
  count: number;
}

/**
 * Execution-state selectors understood by the VM. Keeping them as bytes lets
 * the protocol add selectors without changing this schema.
 */
export interface Inspect {
  state: Buffer;
}

export interface FunctionRef {
  functionIndex: number;
}

export interface ValueUpdate {
  index: number;
  value?: Value | undefined;
}

export interface Snapshot {
  programCounter: number;
  state: State;
  breakpoints: number[];
  functions: FunctionMessage[];
  callstack: CallstackEntry[];
  locals?: Locals | undefined;
  queue?: EventsQueue | undefined;
  callbacks?: CallbackMapping | undefined;
  globals: Value[];
  stack: Value[];
  table?: TableState | undefined;
  memory?: MemoryState | undefined;
  branchTable: number[];
  io: IOState[];
  overrides: Override[];
  heapUsed: number;
}

export interface FunctionMessage {
  functionIndex: number;
  range?: Range | undefined;
  locals?: Locals | undefined;
  instructions: Buffer;
}

export interface RemoteFunctionCall {
  functionIndex: number;
  arguments: Value[];
}

export interface CallstackEntry {
  type: number;
  functionIndex: number;
  stackPointer: number;
  framePointer: number;
  start: number;
  returnAddress: number;
}

export interface Locals {
  values: Value[];
}

/**
 * The oneof tag is the value type, so a separate type enum and a decimal
 * string are unnecessary. Fixed-width fields preserve WebAssembly bits and
 * are fast to construct on the MCU.
 */
export interface Value {
  i32Bits?: number | undefined;
  i64Bits?: bigint | undefined;
  f32Bits?: number | undefined;
  f64Bits?: bigint | undefined;
  raw?: Buffer | undefined;
  index: number;
}

export interface CallbackMapping {
  entries: CallbackEntry[];
}

export interface CallbackEntry {
  topic: string;
  tableIndexes: number[];
}

export interface EventsQueue {
  /** Total events in the queue; this can exceed the returned slice length. */
  totalCount: number;
  events: Event[];
  range?: Range | undefined;
}

export interface Event {
  topic: string;
  payload: Buffer;
}

export interface Range {
  start: number;
  end: number;
}

export interface ModuleUpdate {
  wasm: Buffer;
}

export interface IndexedValues {
  values: Value[];
}

export interface SnapshotPolicyConfig {
  policy: SnapshotPolicy;
  interval: number;
  minimumReturnCount: number;
  selectedState: Buffer;
}

export interface Override {
  primitiveName: string;
  argumentWords: number[];
  result: number;
}

export interface OperationResult {
  command: Command;
  success: boolean;
}

export interface RemoteFunctionResult {
  success: boolean;
  results: Value[];
  error: Buffer;
}

export interface Checkpoint {
  instructionCount: number;
  hasPrimitiveCall: boolean;
  primitiveFunctionIndex: number;
  arguments: Value[];
  results: Value[];
  snapshot?: Snapshot | undefined;
}

export interface TableState {
  initial: number;
  maximum: number;
  entries: number[];
}

export interface MemoryState {
  initial: number;
  maximum: number;
  pages: number;
  bytes: Buffer;
}

export interface IOState {
  key: string;
  output: boolean;
  value: number;
}

function createBaseCodeLocation(): CodeLocation {
  return { moduleIndex: 0, programCounter: 0 };
}

export const CodeLocation: MessageFns<CodeLocation> = {
  encode(message: CodeLocation, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.moduleIndex !== 0) {
      writer.uint32(8).uint32(message.moduleIndex);
    }
    if (message.programCounter !== 0) {
      writer.uint32(16).uint32(message.programCounter);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CodeLocation {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCodeLocation();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.moduleIndex = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.programCounter = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<CodeLocation>, I>>(base?: I): CodeLocation {
    return CodeLocation.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CodeLocation>, I>>(object: I): CodeLocation {
    const message = createBaseCodeLocation();
    message.moduleIndex = object.moduleIndex ?? 0;
    message.programCounter = object.programCounter ?? 0;
    return message;
  },
};

function createBaseBreakpoint(): Breakpoint {
  return { location: undefined };
}

export const Breakpoint: MessageFns<Breakpoint> = {
  encode(message: Breakpoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.location !== undefined) {
      CodeLocation.encode(message.location, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Breakpoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseBreakpoint();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.location = CodeLocation.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<Breakpoint>, I>>(base?: I): Breakpoint {
    return Breakpoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Breakpoint>, I>>(object: I): Breakpoint {
    const message = createBaseBreakpoint();
    message.location = (object.location !== undefined && object.location !== null)
      ? CodeLocation.fromPartial(object.location)
      : undefined;
    return message;
  },
};

function createBaseHitBreakpoint(): HitBreakpoint {
  return { location: undefined };
}

export const HitBreakpoint: MessageFns<HitBreakpoint> = {
  encode(message: HitBreakpoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.location !== undefined) {
      CodeLocation.encode(message.location, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): HitBreakpoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseHitBreakpoint();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.location = CodeLocation.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<HitBreakpoint>, I>>(base?: I): HitBreakpoint {
    return HitBreakpoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<HitBreakpoint>, I>>(object: I): HitBreakpoint {
    const message = createBaseHitBreakpoint();
    message.location = (object.location !== undefined && object.location !== null)
      ? CodeLocation.fromPartial(object.location)
      : undefined;
    return message;
  },
};

function createBaseNewEvent(): NewEvent {
  return {};
}

export const NewEvent: MessageFns<NewEvent> = {
  encode(_: NewEvent, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NewEvent {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseNewEvent();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<NewEvent>, I>>(base?: I): NewEvent {
    return NewEvent.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NewEvent>, I>>(_: I): NewEvent {
    const message = createBaseNewEvent();
    return message;
  },
};

function createBaseContinueFor(): ContinueFor {
  return { count: 0 };
}

export const ContinueFor: MessageFns<ContinueFor> = {
  encode(message: ContinueFor, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.count !== 0) {
      writer.uint32(8).uint32(message.count);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ContinueFor {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseContinueFor();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.count = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<ContinueFor>, I>>(base?: I): ContinueFor {
    return ContinueFor.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ContinueFor>, I>>(object: I): ContinueFor {
    const message = createBaseContinueFor();
    message.count = object.count ?? 0;
    return message;
  },
};

function createBaseInspect(): Inspect {
  return { state: Buffer.alloc(0) };
}

export const Inspect: MessageFns<Inspect> = {
  encode(message: Inspect, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.state.length !== 0) {
      writer.uint32(10).bytes(message.state);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Inspect {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseInspect();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.state = Buffer.from(reader.bytes());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<Inspect>, I>>(base?: I): Inspect {
    return Inspect.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Inspect>, I>>(object: I): Inspect {
    const message = createBaseInspect();
    message.state = object.state ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseFunctionRef(): FunctionRef {
  return { functionIndex: 0 };
}

export const FunctionRef: MessageFns<FunctionRef> = {
  encode(message: FunctionRef, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.functionIndex !== 0) {
      writer.uint32(8).uint32(message.functionIndex);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FunctionRef {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseFunctionRef();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.functionIndex = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<FunctionRef>, I>>(base?: I): FunctionRef {
    return FunctionRef.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FunctionRef>, I>>(object: I): FunctionRef {
    const message = createBaseFunctionRef();
    message.functionIndex = object.functionIndex ?? 0;
    return message;
  },
};

function createBaseValueUpdate(): ValueUpdate {
  return { index: 0, value: undefined };
}

export const ValueUpdate: MessageFns<ValueUpdate> = {
  encode(message: ValueUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.index !== 0) {
      writer.uint32(8).uint32(message.index);
    }
    if (message.value !== undefined) {
      Value.encode(message.value, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ValueUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseValueUpdate();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.index = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.value = Value.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<ValueUpdate>, I>>(base?: I): ValueUpdate {
    return ValueUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ValueUpdate>, I>>(object: I): ValueUpdate {
    const message = createBaseValueUpdate();
    message.index = object.index ?? 0;
    message.value = (object.value !== undefined && object.value !== null) ? Value.fromPartial(object.value) : undefined;
    return message;
  },
};

function createBaseSnapshot(): Snapshot {
  return {
    programCounter: 0,
    state: 0,
    breakpoints: [],
    functions: [],
    callstack: [],
    locals: undefined,
    queue: undefined,
    callbacks: undefined,
    globals: [],
    stack: [],
    table: undefined,
    memory: undefined,
    branchTable: [],
    io: [],
    overrides: [],
    heapUsed: 0,
  };
}

export const Snapshot: MessageFns<Snapshot> = {
  encode(message: Snapshot, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.programCounter !== 0) {
      writer.uint32(8).uint32(message.programCounter);
    }
    if (message.state !== 0) {
      writer.uint32(16).int32(message.state);
    }
    writer.uint32(26).fork();
    for (const v of message.breakpoints) {
      writer.uint32(v);
    }
    writer.join();
    for (const v of message.functions) {
      FunctionMessage.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.callstack) {
      CallstackEntry.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.locals !== undefined) {
      Locals.encode(message.locals, writer.uint32(50).fork()).join();
    }
    if (message.queue !== undefined) {
      EventsQueue.encode(message.queue, writer.uint32(58).fork()).join();
    }
    if (message.callbacks !== undefined) {
      CallbackMapping.encode(message.callbacks, writer.uint32(66).fork()).join();
    }
    for (const v of message.globals) {
      Value.encode(v!, writer.uint32(74).fork()).join();
    }
    for (const v of message.stack) {
      Value.encode(v!, writer.uint32(82).fork()).join();
    }
    if (message.table !== undefined) {
      TableState.encode(message.table, writer.uint32(90).fork()).join();
    }
    if (message.memory !== undefined) {
      MemoryState.encode(message.memory, writer.uint32(98).fork()).join();
    }
    writer.uint32(106).fork();
    for (const v of message.branchTable) {
      writer.uint32(v);
    }
    writer.join();
    for (const v of message.io) {
      IOState.encode(v!, writer.uint32(114).fork()).join();
    }
    for (const v of message.overrides) {
      Override.encode(v!, writer.uint32(122).fork()).join();
    }
    if (message.heapUsed !== 0) {
      writer.uint32(128).uint32(message.heapUsed);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Snapshot {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSnapshot();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.programCounter = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.state = reader.int32() as any;
            continue;
          }
          case 3: {
            if (tag === 24) {
              message.breakpoints.push(reader.uint32());

              continue;
            }

            if (tag === 26) {
              const end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) {
                message.breakpoints.push(reader.uint32());
              }

              continue;
            }

            break;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.functions.push(FunctionMessage.decode(reader, reader.uint32()));
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.callstack.push(CallstackEntry.decode(reader, reader.uint32()));
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.locals = Locals.decode(reader, reader.uint32());
            continue;
          }
          case 7: {
            if (tag !== 58) {
              break;
            }

            message.queue = EventsQueue.decode(reader, reader.uint32());
            continue;
          }
          case 8: {
            if (tag !== 66) {
              break;
            }

            message.callbacks = CallbackMapping.decode(reader, reader.uint32());
            continue;
          }
          case 9: {
            if (tag !== 74) {
              break;
            }

            message.globals.push(Value.decode(reader, reader.uint32()));
            continue;
          }
          case 10: {
            if (tag !== 82) {
              break;
            }

            message.stack.push(Value.decode(reader, reader.uint32()));
            continue;
          }
          case 11: {
            if (tag !== 90) {
              break;
            }

            message.table = TableState.decode(reader, reader.uint32());
            continue;
          }
          case 12: {
            if (tag !== 98) {
              break;
            }

            message.memory = MemoryState.decode(reader, reader.uint32());
            continue;
          }
          case 13: {
            if (tag === 104) {
              message.branchTable.push(reader.uint32());

              continue;
            }

            if (tag === 106) {
              const end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) {
                message.branchTable.push(reader.uint32());
              }

              continue;
            }

            break;
          }
          case 14: {
            if (tag !== 114) {
              break;
            }

            message.io.push(IOState.decode(reader, reader.uint32()));
            continue;
          }
          case 15: {
            if (tag !== 122) {
              break;
            }

            message.overrides.push(Override.decode(reader, reader.uint32()));
            continue;
          }
          case 16: {
            if (tag !== 128) {
              break;
            }

            message.heapUsed = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<Snapshot>, I>>(base?: I): Snapshot {
    return Snapshot.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Snapshot>, I>>(object: I): Snapshot {
    const message = createBaseSnapshot();
    message.programCounter = object.programCounter ?? 0;
    message.state = object.state ?? 0;
    message.breakpoints = object.breakpoints?.map((e) => e) || [];
    message.functions = object.functions?.map((e) => FunctionMessage.fromPartial(e)) || [];
    message.callstack = object.callstack?.map((e) => CallstackEntry.fromPartial(e)) || [];
    message.locals = (object.locals !== undefined && object.locals !== null)
      ? Locals.fromPartial(object.locals)
      : undefined;
    message.queue = (object.queue !== undefined && object.queue !== null)
      ? EventsQueue.fromPartial(object.queue)
      : undefined;
    message.callbacks = (object.callbacks !== undefined && object.callbacks !== null)
      ? CallbackMapping.fromPartial(object.callbacks)
      : undefined;
    message.globals = object.globals?.map((e) => Value.fromPartial(e)) || [];
    message.stack = object.stack?.map((e) => Value.fromPartial(e)) || [];
    message.table = (object.table !== undefined && object.table !== null)
      ? TableState.fromPartial(object.table)
      : undefined;
    message.memory = (object.memory !== undefined && object.memory !== null)
      ? MemoryState.fromPartial(object.memory)
      : undefined;
    message.branchTable = object.branchTable?.map((e) => e) || [];
    message.io = object.io?.map((e) => IOState.fromPartial(e)) || [];
    message.overrides = object.overrides?.map((e) => Override.fromPartial(e)) || [];
    message.heapUsed = object.heapUsed ?? 0;
    return message;
  },
};

function createBaseFunctionMessage(): FunctionMessage {
  return { functionIndex: 0, range: undefined, locals: undefined, instructions: Buffer.alloc(0) };
}

export const FunctionMessage: MessageFns<FunctionMessage> = {
  encode(message: FunctionMessage, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.functionIndex !== 0) {
      writer.uint32(8).uint32(message.functionIndex);
    }
    if (message.range !== undefined) {
      Range.encode(message.range, writer.uint32(18).fork()).join();
    }
    if (message.locals !== undefined) {
      Locals.encode(message.locals, writer.uint32(26).fork()).join();
    }
    if (message.instructions.length !== 0) {
      writer.uint32(34).bytes(message.instructions);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): FunctionMessage {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseFunctionMessage();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.functionIndex = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.range = Range.decode(reader, reader.uint32());
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.locals = Locals.decode(reader, reader.uint32());
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.instructions = Buffer.from(reader.bytes());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<FunctionMessage>, I>>(base?: I): FunctionMessage {
    return FunctionMessage.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<FunctionMessage>, I>>(object: I): FunctionMessage {
    const message = createBaseFunctionMessage();
    message.functionIndex = object.functionIndex ?? 0;
    message.range = (object.range !== undefined && object.range !== null) ? Range.fromPartial(object.range) : undefined;
    message.locals = (object.locals !== undefined && object.locals !== null)
      ? Locals.fromPartial(object.locals)
      : undefined;
    message.instructions = object.instructions ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseRemoteFunctionCall(): RemoteFunctionCall {
  return { functionIndex: 0, arguments: [] };
}

export const RemoteFunctionCall: MessageFns<RemoteFunctionCall> = {
  encode(message: RemoteFunctionCall, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.functionIndex !== 0) {
      writer.uint32(8).uint32(message.functionIndex);
    }
    for (const v of message.arguments) {
      Value.encode(v!, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoteFunctionCall {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRemoteFunctionCall();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.functionIndex = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.arguments.push(Value.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<RemoteFunctionCall>, I>>(base?: I): RemoteFunctionCall {
    return RemoteFunctionCall.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemoteFunctionCall>, I>>(object: I): RemoteFunctionCall {
    const message = createBaseRemoteFunctionCall();
    message.functionIndex = object.functionIndex ?? 0;
    message.arguments = object.arguments?.map((e) => Value.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCallstackEntry(): CallstackEntry {
  return { type: 0, functionIndex: 0, stackPointer: 0, framePointer: 0, start: 0, returnAddress: 0 };
}

export const CallstackEntry: MessageFns<CallstackEntry> = {
  encode(message: CallstackEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.type !== 0) {
      writer.uint32(8).uint32(message.type);
    }
    if (message.functionIndex !== 0) {
      writer.uint32(16).uint32(message.functionIndex);
    }
    if (message.stackPointer !== 0) {
      writer.uint32(24).uint32(message.stackPointer);
    }
    if (message.framePointer !== 0) {
      writer.uint32(32).uint32(message.framePointer);
    }
    if (message.start !== 0) {
      writer.uint32(40).uint32(message.start);
    }
    if (message.returnAddress !== 0) {
      writer.uint32(48).uint32(message.returnAddress);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CallstackEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCallstackEntry();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.type = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.functionIndex = reader.uint32();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.stackPointer = reader.uint32();
            continue;
          }
          case 4: {
            if (tag !== 32) {
              break;
            }

            message.framePointer = reader.uint32();
            continue;
          }
          case 5: {
            if (tag !== 40) {
              break;
            }

            message.start = reader.uint32();
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.returnAddress = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<CallstackEntry>, I>>(base?: I): CallstackEntry {
    return CallstackEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CallstackEntry>, I>>(object: I): CallstackEntry {
    const message = createBaseCallstackEntry();
    message.type = object.type ?? 0;
    message.functionIndex = object.functionIndex ?? 0;
    message.stackPointer = object.stackPointer ?? 0;
    message.framePointer = object.framePointer ?? 0;
    message.start = object.start ?? 0;
    message.returnAddress = object.returnAddress ?? 0;
    return message;
  },
};

function createBaseLocals(): Locals {
  return { values: [] };
}

export const Locals: MessageFns<Locals> = {
  encode(message: Locals, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.values) {
      Value.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Locals {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseLocals();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.values.push(Value.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<Locals>, I>>(base?: I): Locals {
    return Locals.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Locals>, I>>(object: I): Locals {
    const message = createBaseLocals();
    message.values = object.values?.map((e) => Value.fromPartial(e)) || [];
    return message;
  },
};

function createBaseValue(): Value {
  return { i32Bits: undefined, i64Bits: undefined, f32Bits: undefined, f64Bits: undefined, raw: undefined, index: 0 };
}

export const Value: MessageFns<Value> = {
  encode(message: Value, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.i32Bits !== undefined) {
      writer.uint32(13).fixed32(message.i32Bits);
    }
    if (message.i64Bits !== undefined) {
      if (BigInt.asUintN(64, message.i64Bits) !== message.i64Bits) {
        throw new globalThis.Error("value provided for field message.i64Bits of type fixed64 too large");
      }
      writer.uint32(17).fixed64(message.i64Bits);
    }
    if (message.f32Bits !== undefined) {
      writer.uint32(29).fixed32(message.f32Bits);
    }
    if (message.f64Bits !== undefined) {
      if (BigInt.asUintN(64, message.f64Bits) !== message.f64Bits) {
        throw new globalThis.Error("value provided for field message.f64Bits of type fixed64 too large");
      }
      writer.uint32(33).fixed64(message.f64Bits);
    }
    if (message.raw !== undefined) {
      writer.uint32(42).bytes(message.raw);
    }
    if (message.index !== 0) {
      writer.uint32(48).uint32(message.index);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Value {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseValue();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 13) {
              break;
            }

            message.i32Bits = reader.fixed32();
            continue;
          }
          case 2: {
            if (tag !== 17) {
              break;
            }

            message.i64Bits = reader.fixed64() as bigint;
            continue;
          }
          case 3: {
            if (tag !== 29) {
              break;
            }

            message.f32Bits = reader.fixed32();
            continue;
          }
          case 4: {
            if (tag !== 33) {
              break;
            }

            message.f64Bits = reader.fixed64() as bigint;
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.raw = Buffer.from(reader.bytes());
            continue;
          }
          case 6: {
            if (tag !== 48) {
              break;
            }

            message.index = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<Value>, I>>(base?: I): Value {
    return Value.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Value>, I>>(object: I): Value {
    const message = createBaseValue();
    message.i32Bits = object.i32Bits ?? undefined;
    message.i64Bits = (object.i64Bits !== undefined && object.i64Bits !== null) ? BigInt(object.i64Bits) : undefined;
    message.f32Bits = object.f32Bits ?? undefined;
    message.f64Bits = (object.f64Bits !== undefined && object.f64Bits !== null) ? BigInt(object.f64Bits) : undefined;
    message.raw = object.raw ?? undefined;
    message.index = object.index ?? 0;
    return message;
  },
};

function createBaseCallbackMapping(): CallbackMapping {
  return { entries: [] };
}

export const CallbackMapping: MessageFns<CallbackMapping> = {
  encode(message: CallbackMapping, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.entries) {
      CallbackEntry.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CallbackMapping {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCallbackMapping();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.entries.push(CallbackEntry.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<CallbackMapping>, I>>(base?: I): CallbackMapping {
    return CallbackMapping.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CallbackMapping>, I>>(object: I): CallbackMapping {
    const message = createBaseCallbackMapping();
    message.entries = object.entries?.map((e) => CallbackEntry.fromPartial(e)) || [];
    return message;
  },
};

function createBaseCallbackEntry(): CallbackEntry {
  return { topic: "", tableIndexes: [] };
}

export const CallbackEntry: MessageFns<CallbackEntry> = {
  encode(message: CallbackEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.topic !== "") {
      writer.uint32(10).string(message.topic);
    }
    writer.uint32(18).fork();
    for (const v of message.tableIndexes) {
      writer.uint32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CallbackEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCallbackEntry();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.topic = reader.string();
            continue;
          }
          case 2: {
            if (tag === 16) {
              message.tableIndexes.push(reader.uint32());

              continue;
            }

            if (tag === 18) {
              const end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) {
                message.tableIndexes.push(reader.uint32());
              }

              continue;
            }

            break;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<CallbackEntry>, I>>(base?: I): CallbackEntry {
    return CallbackEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CallbackEntry>, I>>(object: I): CallbackEntry {
    const message = createBaseCallbackEntry();
    message.topic = object.topic ?? "";
    message.tableIndexes = object.tableIndexes?.map((e) => e) || [];
    return message;
  },
};

function createBaseEventsQueue(): EventsQueue {
  return { totalCount: 0, events: [], range: undefined };
}

export const EventsQueue: MessageFns<EventsQueue> = {
  encode(message: EventsQueue, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.totalCount !== 0) {
      writer.uint32(8).uint32(message.totalCount);
    }
    for (const v of message.events) {
      Event.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.range !== undefined) {
      Range.encode(message.range, writer.uint32(26).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): EventsQueue {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEventsQueue();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.totalCount = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.events.push(Event.decode(reader, reader.uint32()));
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.range = Range.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<EventsQueue>, I>>(base?: I): EventsQueue {
    return EventsQueue.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<EventsQueue>, I>>(object: I): EventsQueue {
    const message = createBaseEventsQueue();
    message.totalCount = object.totalCount ?? 0;
    message.events = object.events?.map((e) => Event.fromPartial(e)) || [];
    message.range = (object.range !== undefined && object.range !== null) ? Range.fromPartial(object.range) : undefined;
    return message;
  },
};

function createBaseEvent(): Event {
  return { topic: "", payload: Buffer.alloc(0) };
}

export const Event: MessageFns<Event> = {
  encode(message: Event, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.topic !== "") {
      writer.uint32(10).string(message.topic);
    }
    if (message.payload.length !== 0) {
      writer.uint32(18).bytes(message.payload);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Event {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseEvent();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.topic = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.payload = Buffer.from(reader.bytes());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<Event>, I>>(base?: I): Event {
    return Event.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Event>, I>>(object: I): Event {
    const message = createBaseEvent();
    message.topic = object.topic ?? "";
    message.payload = object.payload ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseRange(): Range {
  return { start: 0, end: 0 };
}

export const Range: MessageFns<Range> = {
  encode(message: Range, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.start !== 0) {
      writer.uint32(8).uint32(message.start);
    }
    if (message.end !== 0) {
      writer.uint32(16).uint32(message.end);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Range {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRange();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.start = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.end = reader.uint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<Range>, I>>(base?: I): Range {
    return Range.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Range>, I>>(object: I): Range {
    const message = createBaseRange();
    message.start = object.start ?? 0;
    message.end = object.end ?? 0;
    return message;
  },
};

function createBaseModuleUpdate(): ModuleUpdate {
  return { wasm: Buffer.alloc(0) };
}

export const ModuleUpdate: MessageFns<ModuleUpdate> = {
  encode(message: ModuleUpdate, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.wasm.length !== 0) {
      writer.uint32(10).bytes(message.wasm);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ModuleUpdate {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseModuleUpdate();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.wasm = Buffer.from(reader.bytes());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<ModuleUpdate>, I>>(base?: I): ModuleUpdate {
    return ModuleUpdate.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ModuleUpdate>, I>>(object: I): ModuleUpdate {
    const message = createBaseModuleUpdate();
    message.wasm = object.wasm ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseIndexedValues(): IndexedValues {
  return { values: [] };
}

export const IndexedValues: MessageFns<IndexedValues> = {
  encode(message: IndexedValues, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.values) {
      Value.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IndexedValues {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseIndexedValues();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.values.push(Value.decode(reader, reader.uint32()));
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<IndexedValues>, I>>(base?: I): IndexedValues {
    return IndexedValues.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IndexedValues>, I>>(object: I): IndexedValues {
    const message = createBaseIndexedValues();
    message.values = object.values?.map((e) => Value.fromPartial(e)) || [];
    return message;
  },
};

function createBaseSnapshotPolicyConfig(): SnapshotPolicyConfig {
  return { policy: 0, interval: 0, minimumReturnCount: 0, selectedState: Buffer.alloc(0) };
}

export const SnapshotPolicyConfig: MessageFns<SnapshotPolicyConfig> = {
  encode(message: SnapshotPolicyConfig, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.policy !== 0) {
      writer.uint32(8).int32(message.policy);
    }
    if (message.interval !== 0) {
      writer.uint32(16).uint32(message.interval);
    }
    if (message.minimumReturnCount !== 0) {
      writer.uint32(24).uint32(message.minimumReturnCount);
    }
    if (message.selectedState.length !== 0) {
      writer.uint32(34).bytes(message.selectedState);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): SnapshotPolicyConfig {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseSnapshotPolicyConfig();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.policy = reader.int32() as any;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.interval = reader.uint32();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.minimumReturnCount = reader.uint32();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.selectedState = Buffer.from(reader.bytes());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<SnapshotPolicyConfig>, I>>(base?: I): SnapshotPolicyConfig {
    return SnapshotPolicyConfig.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<SnapshotPolicyConfig>, I>>(object: I): SnapshotPolicyConfig {
    const message = createBaseSnapshotPolicyConfig();
    message.policy = object.policy ?? 0;
    message.interval = object.interval ?? 0;
    message.minimumReturnCount = object.minimumReturnCount ?? 0;
    message.selectedState = object.selectedState ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseOverride(): Override {
  return { primitiveName: "", argumentWords: [], result: 0 };
}

export const Override: MessageFns<Override> = {
  encode(message: Override, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.primitiveName !== "") {
      writer.uint32(10).string(message.primitiveName);
    }
    writer.uint32(18).fork();
    for (const v of message.argumentWords) {
      writer.fixed32(v);
    }
    writer.join();
    if (message.result !== 0) {
      writer.uint32(29).fixed32(message.result);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Override {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseOverride();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.primitiveName = reader.string();
            continue;
          }
          case 2: {
            if (tag === 21) {
              message.argumentWords.push(reader.fixed32());

              continue;
            }

            if (tag === 18) {
              const end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) {
                message.argumentWords.push(reader.fixed32());
              }

              continue;
            }

            break;
          }
          case 3: {
            if (tag !== 29) {
              break;
            }

            message.result = reader.fixed32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<Override>, I>>(base?: I): Override {
    return Override.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Override>, I>>(object: I): Override {
    const message = createBaseOverride();
    message.primitiveName = object.primitiveName ?? "";
    message.argumentWords = object.argumentWords?.map((e) => e) || [];
    message.result = object.result ?? 0;
    return message;
  },
};

function createBaseOperationResult(): OperationResult {
  return { command: 0, success: false };
}

export const OperationResult: MessageFns<OperationResult> = {
  encode(message: OperationResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.command !== 0) {
      writer.uint32(8).int32(message.command);
    }
    if (message.success !== false) {
      writer.uint32(16).bool(message.success);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): OperationResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseOperationResult();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.command = reader.int32() as any;
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.success = reader.bool();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<OperationResult>, I>>(base?: I): OperationResult {
    return OperationResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<OperationResult>, I>>(object: I): OperationResult {
    const message = createBaseOperationResult();
    message.command = object.command ?? 0;
    message.success = object.success ?? false;
    return message;
  },
};

function createBaseRemoteFunctionResult(): RemoteFunctionResult {
  return { success: false, results: [], error: Buffer.alloc(0) };
}

export const RemoteFunctionResult: MessageFns<RemoteFunctionResult> = {
  encode(message: RemoteFunctionResult, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.success !== false) {
      writer.uint32(8).bool(message.success);
    }
    for (const v of message.results) {
      Value.encode(v!, writer.uint32(18).fork()).join();
    }
    if (message.error.length !== 0) {
      writer.uint32(26).bytes(message.error);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): RemoteFunctionResult {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseRemoteFunctionResult();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.success = reader.bool();
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.results.push(Value.decode(reader, reader.uint32()));
            continue;
          }
          case 3: {
            if (tag !== 26) {
              break;
            }

            message.error = Buffer.from(reader.bytes());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<RemoteFunctionResult>, I>>(base?: I): RemoteFunctionResult {
    return RemoteFunctionResult.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<RemoteFunctionResult>, I>>(object: I): RemoteFunctionResult {
    const message = createBaseRemoteFunctionResult();
    message.success = object.success ?? false;
    message.results = object.results?.map((e) => Value.fromPartial(e)) || [];
    message.error = object.error ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseCheckpoint(): Checkpoint {
  return {
    instructionCount: 0,
    hasPrimitiveCall: false,
    primitiveFunctionIndex: 0,
    arguments: [],
    results: [],
    snapshot: undefined,
  };
}

export const Checkpoint: MessageFns<Checkpoint> = {
  encode(message: Checkpoint, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.instructionCount !== 0) {
      writer.uint32(8).uint32(message.instructionCount);
    }
    if (message.hasPrimitiveCall !== false) {
      writer.uint32(16).bool(message.hasPrimitiveCall);
    }
    if (message.primitiveFunctionIndex !== 0) {
      writer.uint32(24).uint32(message.primitiveFunctionIndex);
    }
    for (const v of message.arguments) {
      Value.encode(v!, writer.uint32(34).fork()).join();
    }
    for (const v of message.results) {
      Value.encode(v!, writer.uint32(42).fork()).join();
    }
    if (message.snapshot !== undefined) {
      Snapshot.encode(message.snapshot, writer.uint32(50).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Checkpoint {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseCheckpoint();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.instructionCount = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.hasPrimitiveCall = reader.bool();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.primitiveFunctionIndex = reader.uint32();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.arguments.push(Value.decode(reader, reader.uint32()));
            continue;
          }
          case 5: {
            if (tag !== 42) {
              break;
            }

            message.results.push(Value.decode(reader, reader.uint32()));
            continue;
          }
          case 6: {
            if (tag !== 50) {
              break;
            }

            message.snapshot = Snapshot.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<Checkpoint>, I>>(base?: I): Checkpoint {
    return Checkpoint.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Checkpoint>, I>>(object: I): Checkpoint {
    const message = createBaseCheckpoint();
    message.instructionCount = object.instructionCount ?? 0;
    message.hasPrimitiveCall = object.hasPrimitiveCall ?? false;
    message.primitiveFunctionIndex = object.primitiveFunctionIndex ?? 0;
    message.arguments = object.arguments?.map((e) => Value.fromPartial(e)) || [];
    message.results = object.results?.map((e) => Value.fromPartial(e)) || [];
    message.snapshot = (object.snapshot !== undefined && object.snapshot !== null)
      ? Snapshot.fromPartial(object.snapshot)
      : undefined;
    return message;
  },
};

function createBaseTableState(): TableState {
  return { initial: 0, maximum: 0, entries: [] };
}

export const TableState: MessageFns<TableState> = {
  encode(message: TableState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.initial !== 0) {
      writer.uint32(8).uint32(message.initial);
    }
    if (message.maximum !== 0) {
      writer.uint32(16).uint32(message.maximum);
    }
    writer.uint32(26).fork();
    for (const v of message.entries) {
      writer.uint32(v);
    }
    writer.join();
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): TableState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseTableState();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.initial = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.maximum = reader.uint32();
            continue;
          }
          case 3: {
            if (tag === 24) {
              message.entries.push(reader.uint32());

              continue;
            }

            if (tag === 26) {
              const end2 = reader.uint32() + reader.pos;
              while (reader.pos < end2) {
                message.entries.push(reader.uint32());
              }

              continue;
            }

            break;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<TableState>, I>>(base?: I): TableState {
    return TableState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<TableState>, I>>(object: I): TableState {
    const message = createBaseTableState();
    message.initial = object.initial ?? 0;
    message.maximum = object.maximum ?? 0;
    message.entries = object.entries?.map((e) => e) || [];
    return message;
  },
};

function createBaseMemoryState(): MemoryState {
  return { initial: 0, maximum: 0, pages: 0, bytes: Buffer.alloc(0) };
}

export const MemoryState: MessageFns<MemoryState> = {
  encode(message: MemoryState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.initial !== 0) {
      writer.uint32(8).uint32(message.initial);
    }
    if (message.maximum !== 0) {
      writer.uint32(16).uint32(message.maximum);
    }
    if (message.pages !== 0) {
      writer.uint32(24).uint32(message.pages);
    }
    if (message.bytes.length !== 0) {
      writer.uint32(34).bytes(message.bytes);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): MemoryState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseMemoryState();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 8) {
              break;
            }

            message.initial = reader.uint32();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.maximum = reader.uint32();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.pages = reader.uint32();
            continue;
          }
          case 4: {
            if (tag !== 34) {
              break;
            }

            message.bytes = Buffer.from(reader.bytes());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<MemoryState>, I>>(base?: I): MemoryState {
    return MemoryState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<MemoryState>, I>>(object: I): MemoryState {
    const message = createBaseMemoryState();
    message.initial = object.initial ?? 0;
    message.maximum = object.maximum ?? 0;
    message.pages = object.pages ?? 0;
    message.bytes = object.bytes ?? Buffer.alloc(0);
    return message;
  },
};

function createBaseIOState(): IOState {
  return { key: "", output: false, value: 0 };
}

export const IOState: MessageFns<IOState> = {
  encode(message: IOState, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.output !== false) {
      writer.uint32(16).bool(message.output);
    }
    if (message.value !== 0) {
      writer.uint32(24).sint32(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): IOState {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseIOState();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.key = reader.string();
            continue;
          }
          case 2: {
            if (tag !== 16) {
              break;
            }

            message.output = reader.bool();
            continue;
          }
          case 3: {
            if (tag !== 24) {
              break;
            }

            message.value = reader.sint32();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  create<I extends Exact<DeepPartial<IOState>, I>>(base?: I): IOState {
    return IOState.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<IOState>, I>>(object: I): IOState {
    const message = createBaseIOState();
    message.key = object.key ?? "";
    message.output = object.output ?? false;
    message.value = object.value ?? 0;
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | bigint | undefined;

export type DeepPartial<T> = T extends bigint ? string | number | bigint
  : T extends Builtin ? T : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
