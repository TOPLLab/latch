export enum StyleType {
    plain,
    github
}

export enum Verbosity {
    none,
    minimal,
    short,
    normal,
    more,
    all,
    debug
}

export * from './Reporter';
export * from './ReporterFactory';
