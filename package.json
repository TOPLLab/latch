{
  "name": "latch",
  "description": "A testing language for constraint environments",
  "version": "0.0.1",
  "scripts": {
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run gen-examples",
    "test": "npm run debugtest && npm run primtest",
    "debugtest": "WABT='WABT/build' npx mocha --exit --reporter src/framework/MochaReporter.ts --require ts-node/register --ui bdd ./src/test/suite/debugger.test.ts",
    "spectest": "WABT='WABT/build' CORESUITE='core/' npx mocha --exit --reporter src/framework/MochaReporter.ts --require ts-node/register --ui bdd ./src/suite/spec.test.ts",
    "primtest": "WABT='WABT/build' npx mocha --exit --reporter src/framework/MochaReporter.ts --require ts-node/register --ui bdd ./src/test/suite/primitives.test.ts",
    "lint": "eslint src --ext ts"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/mocha": "^9.1.0",
    "@types/node": "14.x",
    "chai": "^4.3.6",
    "ieee754": "^1.2.1",
    "mocha": "^9.2.1",
    "mqtt": "^4.3.7",
    "serialport": "^10.4.0",
    "ts-node": "^10.5.0",
    "ts-retry-promise": "^0.7.0",
    "typescript": "^4.5.5"
  }
}
